#!/bin/bash

source "`dirname '$0'`/exhibit-common.bash"

kiosk-browser -vvv --kiosk --fullscreen --integration --localhost --serve ~/repos/mima-launchers/html index.html?cfg=mineralien &

PID="${!}"
wait

