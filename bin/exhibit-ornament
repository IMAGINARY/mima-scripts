#!/bin/bash

source "`dirname '$0'`/exhibit-common.bash"

kiosk-browser -vvv --kiosk --fullscreen --localhost --serve ~/repos/mima-launchers/html apps/cindyjs/iornament/index.html &

PID="${!}"
wait


