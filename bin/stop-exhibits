#!/bin/bash
# This script is supposed to kill all running exhibits and exhibit repeaters.
# It is just a draft and I just updloaded it to get idea across.

pkill hidecursor # total termination
pkill --exact repeat-exhibit # total termination
pkill "exhibit-" # restart

exit #ps f

pkill xbanish
pkill kiosk-browser

